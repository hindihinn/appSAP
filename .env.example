# ==============================================
# Fleet Management (appSAP) - Environment Configuration
# ==============================================
# Salin file ini menjadi .env lalu sesuaikan nilainya:
# cp .env.example .env

# --- Database Configuration ---
# Jika menggunakan MySQL di host/server luar: isi host.docker.internal atau IP server
# Jika menggunakan docker-compose.db.yml (MySQL container): isi 'db'
DB_HOST=host.docker.internal
DB_PORT=3310
DB_USER=root
DB_PASSWORD=Itsapcpka25
DB_NAME=fleet_management

# --- JWT Configuration ---
JWT_SECRET=fleet_management_secret_key_2026_sap
JWT_EXPIRES_IN=7d

# --- Service Exposed Ports ---
API_PORT=1200
WEB_PORT=1300
PMA_PORT=8085
